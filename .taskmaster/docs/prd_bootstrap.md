# Branding Pipeline Task Bootstrap

Build and optimize a naming pipeline that:
1. Generates candidate names using OpenRouter.
2. Validates candidates with cheap and expensive checks.
3. Supports campaign sharding and clear progress telemetry.
4. Improves throughput by parallelizing validation safely.
5. Produces actionable logs and run summaries.
